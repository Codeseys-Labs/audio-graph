# Dependencies
node_modules/
# Note: bun.lock is committed (lockfile)

# Build output
dist/
coverage/
src-tauri/target/

# Rust lock file (large, regenerated from Cargo.toml)
src-tauri/Cargo.lock

# ML models (large binary files — only the top-level models/ directory)
/models/

# Generated Tauri schemas (can be regenerated)
src-tauri/gen/

# Environment
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
.claude/

# Playwright MCP scratch output
.playwright-mcp/

# hyperresearch working vault (regenerable; 3.6MB sqlite + per-run notes).
# Decision-grade outputs are copied into docs/ — the vault itself is scratch.
# Root-anchored so it does NOT match the committed docs/research/ tree.
.hyperresearch/
/research/

# Deep-work-loop / workflow scratch dirs (digests, frames, per-run intermediate
# artifacts). The synthesized reports live alongside as un-prefixed files in
# docs/reviews and docs/designs and ARE committed; these _-prefixed dirs are not.
docs/reviews/_*/
docs/designs/_*/
