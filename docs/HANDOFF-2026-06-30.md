# Handoff — audio-graph session 2026-06-30

Branch: `fix/gtk-test-harness-65f0` @ `b55501d` · **114 commits ahead of `master`**, **27 ahead of the pushed tip `c22d564`** (so 27 commits are LOCAL-ONLY, never pushed). PR **#22** is OPEN (draft).

## TL;DR for the next agent

- **Is everything left in seeds? Almost — with 3 exceptions you must know about (below).** The *forward* backlog is in `sd` (77 open). But three things are NOT in seeds: (a) uncommitted seed-state, (b) gitignored artifacts, (c) two human-decision gates.
- **The branch is green** but the work is **not pushed** and the **seed closures are uncommitted**. Don't `git reset --hard` anything until you've read the "Landmines" section.

## What is NOT captured in seeds (read this first)

1. **Uncommitted seed state.** `.seeds/issues.jsonl` has uncommitted working-tree changes — every seed closed/created this session lives only on disk, not in a commit. `sd` writes this git-TRACKED file directly. **A `git reset --hard` or `git checkout` of another tree will WIPE all session seed mutations** (this happened once this session; recovered by re-applying from the work record). **First action for the next agent: `git add .seeds/issues.jsonl && git commit -m "chore(seeds): checkpoint session state"`** so it's durable.
2. **Gitignored artifacts.** All per-wave plans/verdicts under `docs/reviews/_*/` and `docs/designs/_*/`, plus `_preview-harness/` (the Settings UI render harness), are **gitignored** (`docs/reviews/_*/`, `docs/designs/_*/`, `_preview-harness/` in `.gitignore`). They exist on disk but won't transfer via git. The synthesized, committed docs are: `docs/HANDOFF-2026-06-30.md` (this file), `docs/deep-work-log.md`, the ADRs, the `docs/research/spike-*.md` files, and the `docs/reviews/_backlog-wave4-2026-06-30/P8-verification-report.md`.
3. **Two human-decision gates (NOT seeds, blocked on the user):**
   - **Push of PR #22**: the 27 local commits include a squash-rebase, so pushing is a **non-fast-forward to a shared branch** → needs explicit approval (force-push of shared history). Backup ref `backup/pre-squash-7f57f05` preserves the pre-squash lineage.
   - **CI/secret/policy items** (see "Surfaced for approval" below).

## What was built this session (architecture + work, chronological)

### 1. Anonymous analytics (Sentry) — shipped
Opt-in, toggleable "anonymous analytics" via the **raw Sentry Rust SDK 0.48.3** (NOT tauri-plugin-sentry, which pins 0.42). `send_default_pii=false`, a `before_send` scrubber, thread-global OFF via `Hub::main()` + `client.close()`. Independent of file logging (either/both/neither). DSN is a public ingest key (safe to embed). ADR-0023. Files: `src-tauri/src/analytics/mod.rs`, `docs/ops/sentry-observability.md`.

### 2. Windows `--debug` crash (d47b) — fixed, proven green on CI
Heap assertion (`is_block_type_valid`) on Windows debug launch = CRT mismatch: native ML libs (whisper-rs-sys/llama-cpp-sys-2 via cmake) compile `/MD`, Rust `--debug` links `/MDd`. Fix: `CMAKE_MSVC_RUNTIME_LIBRARY=MultiThreadedDebugDLL` + `LLAMA_LIB_PROFILE=Debug` + per-config `/MDd` flag vars. The test-manifest embed (`AUDIOGRAPH_EMBED_WINDOWS_TEST_MANIFEST`) is for cargo-test binaries ONLY (app builds already have a tauri manifest → duplicate = CVT1100/LNK1123). Proven green on real Windows CI (run 28360916441, 33/33).

### 3. Settings modal full refactor — merged, visually verified
The 4945-line `SettingsPage.tsx` monolith → a **178-line shell** + 16 files under `src/components/settings/` (controller hook + context + 8 panels + rail config + Overview/ProductMode/Capability components + Credentials). Modal + **left sidebar nav** (Discord/Slack/VS Code pattern, the user's chosen IA). MUST-KEEP invariants preserved + verified: only-panel-scrolls (`min-height:0` chain), deep-link routing, unsaved confirm-on-close (pinned by a NEW test — GAP-1), Save footer, full keyboard a11y. Headline IA: Credentials promoted to its own rail section, Overview reshaped to summary-that-links, capability cards relocated into provider panels. Rendered + screenshotted via a Tauri-stubbed static-build harness (`_preview-harness/`, gitignored). Blueprint: `docs/designs/_settings-refactor-2026-06-29/`.

### 4. UI design-system decision — audited, consolidated (Phase 0-2)
Question: shadcn/DaisyUI/etc.? **Decision: consolidate on Tailwind-v4 tokens, NOT shadcn** — because **ADR-0016 (accepted 2026-05-30) already chose token-bridged Tailwind v4 over shadcn** and is mid-migration. The "two styling worlds" was the half-done state of that accepted migration. Shipped Phase 0-2: ADR conventions clause, bridged the genuinely-missing tokens (`font-mono`/`font-sans`/`accent-gemini` — the type scale was ALREADY bridged, correcting the audit), magic-number→token sweep, and a **typed closed-variant `Badge`** that fixes a real production bug (open-set `--${status}` class interpolation → unstyled badge for unknown backend statuses). Report: `docs/reviews/_ui-styling-audit-2026-06-29/`.

### 5. Backlog-to-zero — 7 waves + 1 research wave
Drove the backlog 109 → 77 open via tiered deep-work workflows (parallel lane-worktrees: backend-rust / frontend-ts / docs, each with per-item commit+gate + an adversarial critique that re-runs gates). **~30+ seeds resolved.** Highlights:
- **Credential-401 root cause (7fc5)**: migrated keychain keys ignored `credentials.yaml` edits — fixed + tested.
- **Provider content-egress guards (3b9f/7e0b/6a51)**: defense-in-depth — blocked privacy mode refuses content egress at the write primitive across Bedrock/Gemini/Aura/ASR senders, with mutation-verified tests.
- **Pre-existing registry red (c730)**: `realtime_agent.openai_realtime` was marked `Implemented` without a blocked-policy harness — registry test was red at base; fixed → 18/18.
- **Temporal-graph retcon (0966/78d0)**: wired `invalidate_edge`/`supersede_entity` + fixed a `name_index` resurrection bug. **Caveat: the retcon dispatcher still has NO live producer — see seed 2885.**
- **OpenRouter accelerator catalog (61db/7809)**: view-model + wired into LLM Settings, replacing the hardcoded cerebras/groq default.
- **Token consolidation, a11y pass, ADR-0024 (event-sourced notes/graph), provider-policy metadata (sourced-or-explicitly-unknown), 5 research-spike decision docs.**

## Current verified state (gates run by me, not on report)
- `bun run typecheck` → 0 · `bun run check` (biome) → clean · `bun run test` → **719 / 55 files**
- `cargo +1.95.0 clippy --lib --tests --features cloud -- -D warnings` → 0 · provider-registry test **18/18** · 1198+ lib tests
- **TOOLCHAIN: must use `cargo +1.95.0`** — default cargo is 1.88 and FAILS (sysinfo needs 1.95).

## The highest-value OPEN seeds (the next agent should start here)
- **`2885` (P1)** — wire the diarization retcon dispatcher to a live producer. The dispatcher CODE landed (Wave 7) but nothing calls it in production: live flow is `speech/mod.rs:288/1074/2234 → Tauri event → frontend store (append-only)`, never reaching the backend dispatcher. Privacy-relevant (entity merge never fires). Precise fixSpec in the seed.
- **`ac5d`-followup / lockfile policy** — `Cargo.lock` is gitignored; the green build depends on a local `time 0.3.47` pin. A Cargo.toml constraint was added (`time <0.3.48`), but **decide whether to commit `Cargo.lock`** (touches `.gitignore` policy — surfaced, not done).
- **`6444`** — promote the Deepgram→DiarizationSpanRevisionPayload normalizer from test-only to production.

## Surfaced for approval (NOT autonomously actionable — guardrails)
- **CI-altering** (mission: never autonomously alter CI): `2586` Blacksmith release, `b521` Node 24 actions, `85f9` token-lint CI gate, `f3e3` secret-hygiene CI, `8eeb` pipewire PPA pin, `d3d3` VB-CABLE licensing, `0d66` live-audio CI, the `vitest --retry=2` frontend-job retry.
- **Secret/live-credential** (mission: never touch secrets): live provider smokes — Soniox (`319c`/`0b93`/`be03`/`e35f`), Gladia (`226e`), Speechmatics (`eb2e`), AssemblyAI v3 (`f0a3`).
- **Research-spike go/no-go** (decision docs delivered in `docs/research/spike-*.md`): VAD/AEC → **implement**, Azure Speech → **implement**, SurrealDB & Google Chirp → **spike-further**, ElevenLabs Scribe → **watch-only**.
- **Product/legal**: `a32f` SOC2/GDPR checklist. **Real-audio fixtures**: `c237`/`bfcb`/`d042` (need actual audio data).
- **Push PR #22** (non-fast-forward to shared branch).

## Genuinely blocked (26 seeds)
Each `[blocked]` seed has a still-open blocker (verified via the block-graph — 0 stale-blocks). Mostly the large architecture epics: diarization stack, audio-bus refactor, s2s pipeline, notes/graph pipeline, competitive product roadmap. They unblock as their dependencies close.

## Landmines (hard-won this session)
- **`git reset --hard` wipes uncommitted `sd` state** (`.seeds/issues.jsonl` is tracked but sd mutations aren't committed). Commit `.seeds` before any hard reset.
- **`isolation:'worktree'` can silently branch from origin/master** instead of the feature tip — verify base with `git merge-base --is-ancestor <BASE> HEAD`; the canary is a low test count.
- **WSL2 `/mnt/e` is the binding constraint**: vite build 7-10 min, cargo recompiles slow; parallelize by resource class (network-bound research can run concurrent with disk-bound builds; don't stack heavy parallel compiles).
- **A passing test ≠ working production path** — Wave 7's `2113` had a green unit test that called the dispatcher directly; the dispatcher is never called in production. Verify tests exercise the real path.
